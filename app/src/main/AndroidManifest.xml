<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.compraapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VentaConcretada"></activity>
        <activity android:name=".EditarEliminarProducto" />
        <activity android:name=".RegistrarProducto" />
        <activity android:name=".Ventas" />
        <activity android:name=".BusquedaDeProductos" />
        <activity android:name=".MantencionDeProductos" />
        <activity android:name=".CompraConcretada" />
        <activity android:name=".CarritoDeCompras" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopupEditarProductos"  android:theme="@style/AppTheme.popuptheme"/>
    </application>

</manifest>